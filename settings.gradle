include ':mvidroid-common', ':mvidroid', ':mvidroid-debug'

if (!System.env.JITPACK) {
    include ':test-utils', ':sample:app', ':sample:libraries:common', ':sample:libraries:list', ':sample:libraries:details'
}
